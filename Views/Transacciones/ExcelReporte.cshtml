@{
    ViewData["Title"] = "Reporte Excel";

    var hoy = DateTime.Today;
    var haceUnMes = hoy.AddMonths(-1);
    var mesActual = hoy.Month;
    var mesAnterior = haceUnMes.Month;
    var anualidadActual = hoy.Year;
    var mesAnualidadAnterior = haceUnMes.Year;
    var anualidadAnterior = hoy.AddYears(-1);


}

<partial name="_SubMenu" Model="SubMenuTransacciones.Excel"/>

<h1>Exportar transacciones a Excel</h1>

<div class="mb-3">
    <a asp-action="ReporteExcelPorMes" asp-route-mes="@mesActual" asp-route-anualidad="@anualidadActual"
       class="btn btn-success">
        Exportar mes actual
    </a> ~ @hoy.ToString("MMMM yyyy")
</div>

<div class="mb-3">
    <a asp-action="ReporteExcelPorMes" asp-route-mes="@mesAnterior" asp-route-anualidad="@mesAnualidadAnterior"
       class="btn btn-success">
        Exportar mes anterior
    </a> ~ @haceUnMes.ToString("MMMM yyyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorAnualidad" asp-route-anualidad="@anualidadActual"
       class="btn btn-success">
        Exportar año actual
    </a> ~ @anualidadActual
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorAnualidad"  asp-route-anualidad="@anualidadAnterior"
       class="btn btn-success">
        Exportar año anterior
    </a> ~ @hoy.ToString("MMMM yyyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelTodo"
       class="btn btn-success">
        Exportar to'
    </a> 
</div>